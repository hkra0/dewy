# dewy — 树莓派端（main.py / core / api / hardware）运行依赖
# 安装 Python 依赖： pip install -r requirements.txt
# 系统前置工具：
#   - C 扩展编译（RPi.GPIO 等）：sudo apt install -y python3-dev build-essential
#   - 延时视频/高质量动图导出：sudo apt install -y ffmpeg
#
# 注：ESP32 固件（firmware/）依赖由 platformio.ini 管理。

# ---- Web 服务 ----
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0

# ---- MQTT（与 ESP32 节点通信）----
paho-mqtt>=2.0

# ---- 硬件 I/O ----
# 下面两项只在 Linux/ARM 上安装
# I2C 总线
smbus2>=0.4; platform_system == "Linux"
# GPIO 继电器
RPi.GPIO>=0.7; platform_system == "Linux" and platform_machine in "armv6l armv7l aarch64"

# ---- 图像处理 ----
Pillow>=10.0

# ---- 配置解析 ----
tomli>=2.0; python_version < "3.11"

# ==================================================================
# 可选驱动依赖：仅在 hardware_config.toml 中启用了对应传感器时安装
# ==================================================================
# PyYAML>=6.0                          # 使用 hardware_config.yaml 而非 .toml 时
# requests>=2.31                       # drivers/http_sensor.py
# RPi.bme280>=0.2                      # drivers/bme280_sensor.py
# Adafruit_DHT>=1.4                    # drivers/dht_sensor.py
# adafruit-circuitpython-ahtx0>=1.0    # drivers/aht20_sensor.py
# adafruit-blinka>=8.0                 # 同上，提供 board / busio
